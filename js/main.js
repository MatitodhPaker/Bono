function obtener_datos(){
    /* let cursos=[];
    if ($('input:checkbox[name=curso]:checked').is(':checked')) {
      cursos.push($('input:checkbox[name=curso1]:checked').val());
      alert($('input:checkbox[name=curso1]:checked').is(':checked'));
      return false
    }
    if ($('input:checkbox[name=curso]:checked').is(':checked')) {
      cursos.push($('input:checkbox[name=curso2]:checked').val());
      alert($('input:checkbox[name=curso2]:checked').is(':checked'));
    }
    if ($('input:checkbox[name=curso]:checked').is(':checked')) {
      cursos.push($('input:checkbox[name=curso3]:checked').val());
      alert($('input:checkbox[name=curso3]:checked').is(':checked'));
    }
    for (let index = 0; index < cursos.length; index++) {
      console.log(cursos[index]);
      
    } */
    alert('hola mundo');
    return false;
}
